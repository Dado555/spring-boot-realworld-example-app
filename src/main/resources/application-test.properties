spring.datasource.url=jdbc:sqlite::memory:
# Test-only JWT signing key (Step 1.5 / B13). Not a production secret; used only
# when the "test" Spring profile is active, so application.properties can require
# ${JWT_SECRET} from the environment without breaking the default test suite.
jwt.secret=ieEVCCTc8lOfPMFzigm7YmTkx1itwnS1quv240ltgOLQKlu87yxit0FF0fxzgO3B
jwt.sessionTime=86400
